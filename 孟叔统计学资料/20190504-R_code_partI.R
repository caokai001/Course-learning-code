##################################################################################################################################
# Data 2019-05-04
# Author Howard MENG
# E-mail menghaowei1992@126.com
# probability and statitics in bioinformatics Part I
##################################################################################################################################
rm(list=ls())

# ------------------------------------------------------------------------------------>>>>>>>>
# binomial 
# ------------------------------------------------------------------------------------>>>>>>>>
x1 = rbinom(10000,size = 100,prob = 0.2)
hist(x1,xlim = c(0,100),col="blue",probability = T)

x2 = rbinom(10000,size = 100,prob = 0.5)
hist(x2,xlim = c(0,100),col="red",probability = T)

# ------------------------------------------------------------------------------------>>>>>>>>
# possion
# ------------------------------------------------------------------------------------>>>>>>>>
n = 100000
p = 10^(-4)

# use binomial
pbinom(0,size = n,prob = p)
pbinom(1,size = n,prob = p)
pbinom(2,size = n,prob = p)

# use possion
ppois(0,lambda = n*p)
ppois(1,lambda = n*p)
ppois(2,lambda = n*p)




